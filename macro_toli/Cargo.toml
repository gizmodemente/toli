[package]
name = "macro_toli"
version = "0.3.0"
edition = "2024"
description = "Procedural macro for generating callable tools."
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full", "extra-traits"] }
quote = "1.0"
proc-macro2 = "1.0"
